---
import { Picture as PictureBase } from "astro:assets";

const { src, alt = "", width, height } = Astro.props;
---

<PictureBase
  src={src}
  widths={[width, width * 2, width * 3]}
  height={height}
  formats={["webp"]}
  alt={alt}
  class="picture"
/>
